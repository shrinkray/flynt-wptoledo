<flynt-component name="BlockWysiwygSidebar" class="componentSpacing" {{ options.theme ? 'data-theme="' ~ options.theme ~ '"' }}>
  <div class="container" {{ sidebarPosition ? 'data-sidebar-position="' ~ sidebarPosition ~ '"' }}>
    <aside class="sidebar">
      {{ sidebarHtml|e('wp_kses_post') }}
    </aside>
    <div class="content">
      {{ contentHtml|e('wp_kses_post') }}
    </div>
  </div>
</flynt-component>
